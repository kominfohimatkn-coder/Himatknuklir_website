# Root-level reference only. Put actual values in backend/.env and frontend/.env locally,
# or inject them from your secret manager in deployment.

# Backend: see backend/.env.example
# Frontend: see frontend/.env.example
